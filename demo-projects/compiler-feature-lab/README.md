# Compiler Feature Lab

Visual dashboard for compiler, runtime, and generated-kernel probes.

```sh
cargo run -- --present vulkan --frames 1
```

This is useful as a demo or diagnostic app, but it is intentionally outside the
root SDK first-user path.
