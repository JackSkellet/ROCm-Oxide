# Possibilities Window

Visual showcase for generated kernels, launch contracts, module globals,
post-processing, and interactive tabs.

```sh
cargo run -- --present vulkan --frames 300
```

Use `1-4`, arrows, `P`, `C`, `S`, `R`, and Space to switch modes and runtime
checks.
