# Spectral Lattice

Interactive generated-kernel workbench with overlays, screenshots, and CPU,
OpenGL, and Vulkan presentation paths.

```sh
cargo run -- --present vulkan --frames 300
```

Useful options include `--resolution`, `--fps-limit`, `--gpu-work`, `--mode`,
`--output`, and `--present cpu|gl|vulkan`.
