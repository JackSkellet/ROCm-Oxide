# Stress GUI

Bounded visual stress tools for generated 2D and 3D kernels.

```sh
cargo run --bin stress_test_gui -- --present vulkan --frames 300
cargo run --bin stress_3d_gui -- --present vulkan --frames 300
```

Use arrows, PageUp/PageDown, numeric presets, and Space to change workload while
the demos are running.
