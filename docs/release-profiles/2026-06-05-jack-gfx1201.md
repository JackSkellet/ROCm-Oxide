# ROCm-Oxide Release Profile — Jack gfx1201

Date: 2026-06-05

Purpose: known-good SDK preview validation machine for the first-user path,
doctor diagnostics, and offline release gate.

## Machine

| Field | Value |
|---|---|
| Host | `JackCachyOS` |
| Kernel | `Linux JackCachyOS 7.0.10-2-cachyos #1 SMP PREEMPT_DYNAMIC Thu, 28 May 2026 10:26:37 +0000 x86_64 GNU/Linux` |
| GPU arch | `gfx1201` |
| ROCm path | `/opt/rocm` |
| ROCm runtime | `1.18` from `rocminfo` |
| Rust | `rustc 1.98.0-nightly (cced03bfd 2026-05-28)` |
| Cargo | `cargo 1.98.0-nightly (fbb61be30 2026-05-26)` |
| Rust host | `x86_64-unknown-linux-gnu` |
| Rust LLVM | `22.1.6` |

## Doctor

Command:

```sh
cargo run --quiet --manifest-path tools/cargo-rocm-oxide/Cargo.toml -- rocm-oxide doctor --json
```

Result: `pass`

| Count | Value |
|---|---:|
| PASS | 15 |
| WARN | 0 |
| FAIL | 0 |

Passed checks:

- `cargo`
- `rustc channel`
- `amdgcn target`
- `rust-src component`
- `/dev/kfd`
- `tool search order`
- `ROCm llc`
- `llc amdgcn backend`
- `ROCm clang`
- `ROCm llvm-readelf`
- `ROCm llvm-objdump`
- `ROCm rocminfo`
- `GPU architecture`
- `rocm_agent_enumerator`
- `core build probe`

Key ROCm details:

- `ROCm llc`: `/opt/rocm/lib/llvm/bin/llc [ROCM_PATH] (AMD LLVM version 22.0.0git)`
- `ROCm clang`: `/opt/rocm/lib/llvm/bin/clang [ROCM_PATH] (AMD clang version 22.0.0git (/startdir/rocm-llvm f58b06dce1f9c15707c5f808fd002e18c2accf7e))`
- `ROCm rocminfo`: `/opt/rocm/bin/rocminfo [ROCM_PATH] runtime=1.18 arch=gfx1201`
- `rocm_agent_enumerator`: `/opt/rocm/bin/rocm_agent_enumerator [ROCM_PATH] agents=[gfx1201]`

## First-User Path

Command:

```sh
scripts/first-user-path.sh
```

Result: passed.

Validated commands:

- `cargo run --example hello_gpu`
- `cargo run --features device-spike --example hello_gpu_rust`
- `cargo run --manifest-path tools/cargo-rocm-oxide/Cargo.toml -- rocm-oxide doctor`

Key output:

```text
hello_gpu: device 0 (gfx1201)
hello_gpu: 1048576 elements verified — all correct on gfx1201
hello_gpu_rust: device 0 (gfx1201)
hello_gpu_rust: 1048576 elements verified — Rust-authored kernel passed on gfx1201
doctor: all 15 checks passed
first-user path passed
```

## Offline Gate

Command:

```sh
scripts/verify.sh --offline
```

Result: passed.

Covered checks:

- `crates/rocm-oxide-kernel` tests and doctests
- `tools/rocm-oxide-build` tests
- `tools/cargo-rocm-oxide` tests
- `cargo fmt --check`
- shell syntax checks for `scripts/verify.sh`, `scripts/consumer-smoke.sh`, and
  `scripts/first-user-path.sh`
- `cargo package --allow-dirty --no-verify`
- `cargo doc --no-deps`
- `cargo clippy --all-targets -- -D warnings -A clippy::too_many_arguments`

