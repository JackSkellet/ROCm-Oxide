[package]
name = "oxide-boost"
version = "0.1.0"
edition = "2024"
description = "A process launcher for per-app shader/pipeline cache optimization experiments."
license = "MIT OR Apache-2.0"

[dependencies]
arboard = "3.6"
font8x8 = "0.3"
minifb = "0.28"
